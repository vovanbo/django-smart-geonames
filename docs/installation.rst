============
Installation
============

At the command line::

    $ easy_install django-smart-geonames

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-smart-geonames
    $ pip install django-smart-geonames
